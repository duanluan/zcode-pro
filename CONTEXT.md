# 术语表

- **段落间距（paragraph spacing）**：会话中文本块之间的垂直间距——回合与回合、答案内部相邻段落。不含列表/引用/代码块自身留白与段内行高。
- **列表间距（list spacing）**：答案中列表（有序/无序）自身的上下留白。
- **列表项间距（list item spacing）**：列表内相邻列表项之间的间距。
- **引用与代码块间距（quote & code spacing）**：引用、代码块（及表格）自身的上下留白。
- **回答行高（answer line height）**：回答正文的行高，以字号倍数表示。
- **提问行高（question line height）**：用户提问内容的行高，以字号倍数表示（应用默认与回答不同：1.5 对 1.75）。
